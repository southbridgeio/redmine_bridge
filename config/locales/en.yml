# English strings go here for Rails i18n
en:
  redmine_bridge:
    settings_title: Settings
    new_integration: New integration
    deleted: Integration deleted
    delete_error: There was an error on integration delete
    integration:
      name: Name
      key: Key
      connector: Connector
      project: Project
      settings: Settings
      status_settings: Status settings
      priority_settings: Priority settings
      default_project: Default project
      default_project_title: Use this project only for Southbridge monitoring system (check documentation)
      southbridge_integration: Southbridge integration
      southbridge_integration_title: Use this setting only for Southbridge monitoring system (check documentation)
      check_jira_connection: Check JIRA connection
      connection_success: Connection established
      connection_fail: Connection error
      connection_timeout: Connection timeout expired

      mattermost:
        api_url: Mattermost URL
        api_url_example: "e.g.: mm.domain.com"
        username: Bot usernames (splitted by comma)
        token_id: Token ID
        access_token: Access Token
        team_id: Mattermost team ID
        tls_errors: Skip TLS errors
        defaults: Default issue values
        default_tracker_id: Tracker (id)
        default_priority_id: Priority (id)
        default_status_id: Status (id)
        default_assignee_id: Assignee (id)
        initial_message: Initial message
        issue_created: Issue [#%{id}](%{url}) was created.
        unknown_action: Unknown command, please check syntax
        messages_locale: Notifications language
